/**
 * Sampled directly from the TrainerTrade badge (app/assets/icon.png) rather
 * than eyeballed, so the app's UI actually matches the logo instead of just
 * being "close enough."
 */
export const colors = {
  navy: "#0a1e3c",
  navyDark: "#061428",
  gold: "#c9a05c",
  goldLight: "#e8d3a0",

  white: "#ffffff",
  surface: "#f5f6f8",
  border: "#e2e5ea",

  textPrimary: "#0a1e3c",
  textSecondary: "#6b7280",
  textMuted: "#9aa1ab",

  error: "#c0392b",
  errorBg: "#fdecea",
  success: "#2ecc71",
} as const;
